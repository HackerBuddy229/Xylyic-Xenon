@{
    ViewData["Title"] = "Home Page";
}
@model IndexViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<div class="text-center">
    <h2 class="display-4">Your new Project name:</h2>
    <h1 id="projName"><span class="capsule">[</span>@Model.projName<span class="capsule">]</span></h1>

    <div id="reload">
        <form asp-controller="Home" asp-action="Index" method="get">
                <input class="btn btn-secondary btn-color-secondary" type="submit" value="We can do better"/>
        </form>
    </div>
</div>
